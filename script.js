(()=>{'use strict';const menu=document.querySelector('.menu-toggle'),nav=document.querySelector('.nav');const close=()=>{menu?.setAttribute('aria-expanded','false');menu?.setAttribute('aria-label','Open menu');nav?.classList.remove('open')};menu?.addEventListener('click',()=>{const next=menu.getAttribute('aria-expanded')!=='true';menu.setAttribute('aria-expanded',String(next));menu.setAttribute('aria-label',next?'Close menu':'Open menu');nav?.classList.toggle('open',next)});nav?.querySelectorAll('a').forEach(a=>a.addEventListener('click',close));document.addEventListener('keydown',e=>{if(e.key==='Escape')close()});document.addEventListener('pointerdown',e=>{if(nav?.classList.contains('open')&&!nav.contains(e.target)&&!menu?.contains(e.target))close()});const states={morning:{kicker:'START SLOW / COFFEE & BREAKFAST',title:'Good mornings\nstart here.',copy:'Settle in for coffee, breakfast and brunch on Lark Lane. Bring a friend, or bring a little time for yourself.',label:'MORNING',art:'assets/images/coffee.svg'},afternoon:{kicker:'LUNCH / MEDITERRANEAN FLAVOURS',title:'Take the\nlong way round.',copy:'Freshly made Mediterranean-inspired dishes and a leisurely lunch, whatever the afternoon looks like.',label:'AFTERNOON',art:'assets/images/day-plate.svg'},evening:{kicker:'SUPPER / SOMETHING TO SIP',title:'One more\nround of good.',copy:'As the light changes, settle in for evening food, a glass of wine or a classic cocktail.',label:'EVENING',art:'assets/images/glass.svg'}};const stage=document.querySelector('[data-day-stage]');document.querySelectorAll('[data-day]').forEach(button=>button.addEventListener('click',()=>{const key=button.dataset.day,d=states[key];if(!d||!stage)return;document.querySelectorAll('[data-day]').forEach(tab=>{const active=tab===button;tab.classList.toggle('is-active',active);tab.setAttribute('aria-pressed',String(active))});stage.dataset.dayStage=key;stage.querySelector('[data-day-kicker]').textContent=d.kicker;stage.querySelector('[data-day-title]').textContent=d.title;stage.querySelector('[data-day-copy]').textContent=d.copy;stage.querySelector('[data-day-label]').textContent=d.label;stage.querySelector('.day-stage-art img').src=d.art}));document.querySelectorAll('img[data-fallback]').forEach(img=>img.addEventListener('error',()=>{const fallback=img.dataset.fallback;if(fallback&&img.src!==new URL(fallback,document.baseURI).href){img.removeAttribute('data-fallback');img.src=fallback}}));const targets=document.querySelectorAll('.reveal');if(!('IntersectionObserver' in window)||matchMedia('(prefers-reduced-motion: reduce)').matches){targets.forEach(el=>el.classList.add('visible'))}else{const io=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('visible');io.unobserve(entry.target)}}),{threshold:.08});targets.forEach(el=>io.observe(el))}})();